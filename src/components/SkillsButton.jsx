const SkillsButton = ({language}) => {
  return (
    <button className="flex justify-center items-center bg-white text-black p-3 rounded-full">
      <img src="" alt="" />
      <p>{language}</p>
    </button>
  );
};

export default SkillsButton;
